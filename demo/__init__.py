"""Streamlit demo layer for Lab 24 — Production Eval + Guardrail Stack.

Chỉ là lớp trình bày (presentation): mọi số liệu thật đều đến từ
`src/phase_*.py` hoặc `reports/*.json`. Module này không tự tính điểm.
"""
