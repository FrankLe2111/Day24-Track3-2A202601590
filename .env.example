# Required
OPENAI_API_KEY=sk-...

# Optional: HuggingFace token (only if using HF Inference Endpoints)
HF_TOKEN=hf_...

# Optional: OCR scanned PDFs with local Hugging Face GLM-OCR
GLMOCR_ENABLED=1
GLMOCR_MODEL=zai-org/GLM-OCR
